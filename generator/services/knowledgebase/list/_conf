Description = Gets a list of a specific set of knowledgebase entries. Only authenticated users are allowed to access this method. Authentication is done by sending a Basic HTTP Authorisation header. 

Syntax = /v1/knowledgebase/data
Method = GET

[URL Args]
content =
type =
search =
sortmode =
page =
pagesize =

[Statuscodes]
100 = successfull
