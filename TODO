next and prev icon display issue in the frame

+++
   download interface
   register interface

The Return button should be done in the stage level together with history list.
Add more effect like: to the top, to the end
  while there is lots of informations in the info-page


We should make a better search entry...

lots of button animation work to do 


make an application object
which control all store  stage
so donnot use such as gnome_app_stage_get_default
