[Summary]
Description = Set a attribute. Only authenticated users are allowed to access this method. Authentication is done by sending a Basic HTTP Authorisation header. 

Syntax = /v1/privatedata/setattribute/"app"/"key" 
Method = POST

[Args]
app=
key=

[POST_Args]
value =

[Statuscodes]
100 = successfull
